import styled from 'styled-components';

export const TodoListContainer = styled.section`
	padding: 5px 10px;

	span {
		color: #ffffff;
		font-size: 1.4rem;
	}

	ul {
		list-style: none;
		padding-left: 0;
	}
`;
